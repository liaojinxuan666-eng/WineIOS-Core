diff --git a/configure b/configure
index df4c4a9..bc44a12 100755
--- a/configure
+++ b/configure
@@ -6411,7 +6411,7 @@ fi
 
 
 case $host in
-  *-darwin*)
+  *-darwin*|*-ios*)
     with_fontconfig=${with_fontconfig:-no}
     ;;
   *-mingw32*|*-cygwin*)
@@ -11414,6 +11414,28 @@ case $host_os in
     with_pthread=${with_pthread:-no}
     ;;
 
+  ios*)
+    printf "%s\n" "#define WINE_IOS 1" >>confdefs.h
+
+    LIBEXT="dylib"
+    DLLFLAGS="$DLLFLAGS -fPIC"
+    LDDLLFLAGS="-fPIC"
+    enable_winemac_drv=no
+    wine_use_preloader=no
+
+    CORETEXT_LIBS="-framework CoreText"
+
+    COREFOUNDATION_LIBS="-framework CoreFoundation"
+
+    SECURITY_LIBS="-framework Security -framework CoreFoundation"
+
+    SYSTEMCONFIGURATION_LIBS="-framework SystemConfiguration"
+
+    WINELOADER_LDFLAGS=""
+    WINEPRELOADER_LDFLAGS=""
+    UNIXLDFLAGS="-dynamiclib -install_name @rpath/\$(UNIXLIB) -Wl,-rpath,@loader_path\/"
+    ;;
+
   darwin*)
     LIBEXT="dylib"
     DLLFLAGS="$DLLFLAGS -fPIC"
diff --git a/configure.ac b/configure.ac
index b8ae3db..dcaffb8 100644
--- a/configure.ac
+++ b/configure.ac
@@ -108,7 +108,7 @@ AC_DEFINE_UNQUOTED(EXEEXT,["$ac_exeext"],[Define to the file extension for execu
 AC_CHECK_TOOL(LD,ld)
 
 case $host in
-  *-darwin*)
+  *-darwin*|*-ios*)
     with_fontconfig=${with_fontconfig:-no}
     ;;
   *-mingw32*|*-cygwin*)
@@ -921,6 +921,30 @@ case $host_os in
     with_pthread=${with_pthread:-no}
     ;;
 
+  ios*)
+    dnl iOS uses Mach-O and Darwin dylibs, but does not provide the macOS
+    dnl desktop frameworks or the winemac window driver. Keep this platform
+    dnl block intentionally small; individual subsystems are enabled only
+    dnl after their iOS capability probes pass.
+    AC_DEFINE(WINE_IOS,1,[Define to 1 when building the iOS host runtime.])
+    LIBEXT="dylib"
+    DLLFLAGS="$DLLFLAGS -fPIC"
+    LDDLLFLAGS="-fPIC"
+    enable_winemac_drv=no
+    wine_use_preloader=no
+
+    AC_SUBST(CORETEXT_LIBS,"-framework CoreText")
+    AC_SUBST(COREFOUNDATION_LIBS,"-framework CoreFoundation")
+    AC_SUBST(SECURITY_LIBS,"-framework Security -framework CoreFoundation")
+    AC_SUBST(SYSTEMCONFIGURATION_LIBS,"-framework SystemConfiguration")
+
+    dnl The iOS runtime is embedded by the host app. A standalone macOS-style
+    dnl loader and preloader are not linked at this stage.
+    WINELOADER_LDFLAGS=""
+    WINEPRELOADER_LDFLAGS=""
+    UNIXLDFLAGS="-dynamiclib -install_name @rpath/\$(UNIXLIB) -Wl,-rpath,@loader_path\/"
+    ;;
+
   darwin*)
     LIBEXT="dylib"
     DLLFLAGS="$DLLFLAGS -fPIC"
diff --git a/include/config.h.in b/include/config.h.in
index 8b1d33f..fb2ff2f 100644
--- a/include/config.h.in
+++ b/include/config.h.in
@@ -793,6 +793,9 @@
    backward compatibility; new code need not use it. */
 #undef STDC_HEADERS
 
+/* Define to 1 when building the iOS host runtime. */
+#undef WINE_IOS
+
 /* Define if xattr functions take additional arguments (macOS) */
 #undef XATTR_ADDITIONAL_OPTIONS
 
diff --git a/dlls/ntdll/unix/cdrom.c b/dlls/ntdll/unix/cdrom.c
index 0836508..0139d8e 100644
--- a/dlls/ntdll/unix/cdrom.c
+++ b/dlls/ntdll/unix/cdrom.c
@@ -27,6 +27,10 @@
 
 #include "config.h"
 
+#if defined(__APPLE__) && !defined(WINE_IOS)
+#define WINE_MACOS_DESKTOP
+#endif
+
 #include <errno.h>
 #include <string.h>
 #include <stdarg.h>
@@ -75,7 +79,7 @@
 # include <sys/scsiio.h>
 #endif
 
-#ifdef __APPLE__
+#ifdef WINE_MACOS_DESKTOP
 # include <libkern/OSByteOrder.h>
 # include <sys/disk.h>
 # include <IOKit/IOKitLib.h>
@@ -341,7 +345,7 @@ static int CDROM_MediaChanged(int dev)
  * This is ugly and inefficient, but we have no choice since the partition fd doesn't
  * support the eject ioctl.
  */
-#ifdef __APPLE__
+#ifdef WINE_MACOS_DESKTOP
 static NTSTATUS get_parent_device( int fd, char *name, size_t len )
 {
     NTSTATUS status = STATUS_NO_SUCH_FILE;
@@ -521,7 +525,7 @@ static NTSTATUS CDROM_SyncCache(int dev, int fd)
     cdrom_cache[dev].toc_good = 1;
     return STATUS_SUCCESS;
 
-#elif defined(__APPLE__)
+#elif defined(WINE_MACOS_DESKTOP)
     int i;
     dk_cd_read_toc_t hdr;
     CDROM_TOC *toc = &cdrom_cache[dev].toc;
@@ -633,7 +637,7 @@ static BOOL CDROM_GetInterfaceInfo(int fd, UCHAR* iface, UCHAR* port, UCHAR* dev
         }
     }
     return FALSE;
-#elif defined(__APPLE__)
+#elif defined(WINE_MACOS_DESKTOP)
     dk_scsi_identify_t addr;
     if (ioctl(fd, DKIOCSCSIIDENTIFY, &addr) != -1)
     {
@@ -707,7 +711,7 @@ static NTSTATUS CDROM_GetStatusCode(int io)
  */
 static NTSTATUS CDROM_GetControl(int dev, int fd, CDROM_AUDIO_CONTROL* cac)
 {
-#ifdef __APPLE__
+#ifdef WINE_MACOS_DESKTOP
     uint16_t speed;
     int io = ioctl( fd, DKIOCCDGETSPEED, &speed );
     if (io != 0) return CDROM_GetStatusCode( io );
@@ -824,7 +828,7 @@ static NTSTATUS CDROM_SetTray(int fd, BOOL doEject)
     return CDROM_GetStatusCode((ioctl(fd, CDIOCALLOW, NULL)) ||
                                (ioctl(fd, doEject ? CDIOCEJECT : CDIOCCLOSE, NULL)) ||
                                (ioctl(fd, CDIOCPREVENT, NULL)));
-#elif defined(__APPLE__)
+#elif defined(WINE_MACOS_DESKTOP)
     if (doEject) return CDROM_GetStatusCode( ioctl( fd, DKIOCEJECT, NULL ) );
     else return STATUS_NOT_SUPPORTED;
 #else
@@ -1097,7 +1101,7 @@ static NTSTATUS CDROM_ReadQChannel(int dev, int fd, const CDROM_SUB_Q_DATA_FORMA
 
  end:
     ret = CDROM_GetStatusCode(io);
-#elif defined(__APPLE__)
+#elif defined(WINE_MACOS_DESKTOP)
     SUB_Q_HEADER* hdr = (SUB_Q_HEADER*)data;
     int io = 0;
     union
@@ -1161,7 +1165,7 @@ static NTSTATUS CDROM_Verify(int dev, int fd)
         return STATUS_SUCCESS;
     else
         return STATUS_NO_MEDIA_IN_DEVICE;
-#elif defined(__APPLE__)
+#elif defined(WINE_MACOS_DESKTOP)
 	/* At this point, we know that we have media, because in Mac OS X, the
 	 * device file is only created when media is present. */
 	return STATUS_SUCCESS;
@@ -1481,7 +1485,7 @@ static NTSTATUS CDROM_RawRead(int fd, const RAW_READ_INFO* raw, void* buffer, DW
 {
     int         ret = STATUS_NOT_SUPPORTED;
     int         io = -1;
-#ifdef __APPLE__
+#ifdef WINE_MACOS_DESKTOP
     dk_cd_read_t cdrd;
 #endif
 
@@ -1552,7 +1556,7 @@ static NTSTATUS CDROM_RawRead(int fd, const RAW_READ_INFO* raw, void* buffer, DW
         FIXME("NIY: %d\n", raw->TrackMode);
         return STATUS_INVALID_PARAMETER;
     }
-#elif defined(__APPLE__)
+#elif defined(WINE_MACOS_DESKTOP)
     /* Mac OS lets us read multiple parts of the sector at a time.
      * We can read all the sectors in at once, unlike Linux.
      */
@@ -1621,7 +1625,7 @@ static NTSTATUS CDROM_ScsiPassThroughDirect(int fd, const SCSI_PASS_THROUGH_DIRE
 #ifdef HAVE_SG_IO_HDR_T_INTERFACE_ID
     sg_io_hdr_t cmd;
     int io;
-#elif defined __APPLE__
+#elif defined WINE_MACOS_DESKTOP
     dk_scsi_command_t cmd;
     int io;
 #endif
@@ -1675,7 +1679,7 @@ static NTSTATUS CDROM_ScsiPassThroughDirect(int fd, const SCSI_PASS_THROUGH_DIRE
 
     ret = CDROM_GetStatusCode(io);
 
-#elif defined(__APPLE__)
+#elif defined(WINE_MACOS_DESKTOP)
 
     memset(&cmd, 0, sizeof(cmd));
     memcpy(cmd.cdb, in_pkt->Cdb, in_pkt->CdbLength);
@@ -1823,7 +1827,7 @@ static NTSTATUS CDROM_ScsiPassThrough(int fd, const SCSI_PASS_THROUGH *in_pkt, S
 #ifdef HAVE_SG_IO_HDR_T_INTERFACE_ID
     sg_io_hdr_t cmd;
     int io;
-#elif defined __APPLE__
+#elif defined WINE_MACOS_DESKTOP
     dk_scsi_command_t cmd;
     int io;
 #endif
@@ -1880,7 +1884,7 @@ static NTSTATUS CDROM_ScsiPassThrough(int fd, const SCSI_PASS_THROUGH *in_pkt, S
 
     ret = CDROM_GetStatusCode(io);
 
-#elif defined(__APPLE__)
+#elif defined(WINE_MACOS_DESKTOP)
 
     memset(&cmd, 0, sizeof(cmd));
     memcpy(cmd.cdb, in_pkt->Cdb, in_pkt->CdbLength);
@@ -2060,7 +2064,7 @@ static NTSTATUS CDROM_ScsiGetCaps(int fd, PIO_SCSI_CAPABILITIES caps)
     caps->AdapterScansDown = FALSE; /* FIXME ? */
     caps->AdapterUsesPio = FALSE; /* FIXME ? */
     ret = STATUS_SUCCESS;
-#elif defined __APPLE__
+#elif defined WINE_MACOS_DESKTOP
     uint64_t bytesr, bytesw, align;
     int io = ioctl(fd, DKIOCGETMAXBYTECOUNTREAD, &bytesr);
     if (io != 0) return CDROM_GetStatusCode(io);
@@ -2122,7 +2126,7 @@ static NTSTATUS DVD_StartSession(int fd, const DVD_SESSION_ID *sid_in, PDVD_SESS
     ret =CDROM_GetStatusCode(ioctl(fd, DVD_AUTH, &auth_info));
     *sid_out = auth_info.lsa.agid;
     return ret;
-#elif defined(__APPLE__)
+#elif defined(WINE_MACOS_DESKTOP)
     NTSTATUS ret = STATUS_NOT_SUPPORTED;
     dk_dvd_report_key_t dvdrk;
     DVDAuthenticationGrantIDInfo agid_info;
@@ -2158,7 +2162,7 @@ static NTSTATUS DVD_EndSession(int fd, const DVD_SESSION_ID *sid)
 
     TRACE("\n");
     return CDROM_GetStatusCode(ioctl(fd, DVD_AUTH, &auth_info));
-#elif defined(__APPLE__)
+#elif defined(WINE_MACOS_DESKTOP)
     dk_dvd_send_key_t dvdsk;
 
     dvdsk.format = kDVDKeyFormatAGID_Invalidate;
@@ -2207,7 +2211,7 @@ static NTSTATUS DVD_SendKey(int fd, const DVD_COPY_PROTECT_KEY *key)
 	FIXME("Unknown Keytype 0x%x\n",key->KeyType);
     }
     return ret;
-#elif defined(__APPLE__)
+#elif defined(WINE_MACOS_DESKTOP)
     dk_dvd_send_key_t dvdsk;
     union
     {
@@ -2342,7 +2346,7 @@ static NTSTATUS DVD_ReadKey(int fd, PDVD_COPY_PROTECT_KEY key)
 	FIXME("Unknown keytype 0x%x\n",key->KeyType);
     }
     return ret;
-#elif defined(__APPLE__)
+#elif defined(WINE_MACOS_DESKTOP)
     union
     {
         dk_dvd_report_key_t key;
@@ -2529,7 +2533,7 @@ static NTSTATUS DVD_GetRegion(int fd, PDVD_REGION region)
         }
     }
     return ret;
-#elif defined(__APPLE__)
+#elif defined(WINE_MACOS_DESKTOP)
     dk_dvd_report_key_t key;
     dk_dvd_read_structure_t dvd;
     DVDRegionPlaybackControlInfo rpc;
@@ -2709,7 +2713,7 @@ static NTSTATUS DVD_ReadStructure(int dev, const DVD_READ_STRUCTURE *structure,
     case DvdMaxDescriptor: /* Suppress warning */
 	break;
     }
-#elif defined(__APPLE__)
+#elif defined(WINE_MACOS_DESKTOP)
     NTSTATUS ret;
     dk_dvd_read_structure_t dvdrs;
     union
@@ -2916,7 +2920,7 @@ NTSTATUS cdrom_DeviceIoControl( HANDLE device, HANDLE event, PIO_APC_ROUTINE apc
         return status;
     }
 
-#ifdef __APPLE__
+#ifdef WINE_MACOS_DESKTOP
     {
         char name[100];
 
diff --git a/dlls/ntdll/unix/file.c b/dlls/ntdll/unix/file.c
index 42c8253..acfdb8e 100644
--- a/dlls/ntdll/unix/file.c
+++ b/dlls/ntdll/unix/file.c
@@ -2570,7 +2570,7 @@ done:
 #endif /* VFAT_IOCTL_READDIR_BOTH */
 
 
-#ifdef HAVE_GETATTRLIST
+#if defined(HAVE_GETATTRLIST) && !defined(WINE_IOS)
 /***********************************************************************
  *           read_directory_getattrlist
  *
@@ -2611,7 +2611,7 @@ static NTSTATUS read_directory_data_getattrlist( struct dir_data *data, const ch
 
     return STATUS_SUCCESS;
 }
-#endif  /* HAVE_GETATTRLIST */
+#endif  /* HAVE_GETATTRLIST && !WINE_IOS */
 
 
 /***********************************************************************
@@ -2686,7 +2686,7 @@ static NTSTATUS read_directory_data( struct dir_data *data, int fd, const UNICOD
         if (ret > 0)
         {
             unix_name[ret] = 0;
-#ifdef HAVE_GETATTRLIST
+#if defined(HAVE_GETATTRLIST) && !defined(WINE_IOS)
             if (!(status = read_directory_data_getattrlist( data, unix_name ))) return status;
 #endif
             if (!(status = read_directory_data_stat( data, unix_name ))) return status;
@@ -7178,7 +7178,7 @@ NTSTATUS get_device_info( int fd, FILE_FS_DEVICE_INFORMATION *info )
             info->DeviceType = FILE_DEVICE_TAPE;
             break;
         }
-#elif defined(__FreeBSD__) || defined(__FreeBSD_kernel__) || defined(__APPLE__)
+#elif defined(__FreeBSD__) || defined(__FreeBSD_kernel__) || (defined(__APPLE__) && !defined(WINE_IOS))
         {
             int d_type;
             if (ioctl(fd, FIODTYPE, &d_type) == 0)
diff --git a/dlls/ntdll/unix/loader.c b/dlls/ntdll/unix/loader.c
index cb700dd..46ce5ec 100644
--- a/dlls/ntdll/unix/loader.c
+++ b/dlls/ntdll/unix/loader.c
@@ -2048,7 +2048,7 @@ jint JNI_OnLoad( JavaVM *vm, void *reserved )
 
 #endif  /* __ANDROID__ */
 
-#ifdef __APPLE__
+#if defined(__APPLE__) && !defined(WINE_IOS)
 static void *apple_wine_thread( void *arg )
 {
     start_main_thread();
@@ -2297,7 +2297,7 @@ DECLSPEC_EXPORT void __wine_main( int argc, char *argv[] )
     virtual_init();
     init_environment();
 
-#ifdef __APPLE__
+#if defined(__APPLE__) && !defined(WINE_IOS)
     apple_main_thread();
 #endif
     start_main_thread();
diff --git a/dlls/ntdll/unix/server.c b/dlls/ntdll/unix/server.c
index 258a959..b792045 100644
--- a/dlls/ntdll/unix/server.c
+++ b/dlls/ntdll/unix/server.c
@@ -1472,6 +1472,7 @@ static int server_connect(void)
 #ifdef __APPLE__
 #include <mach/mach.h>
 #include <mach/mach_error.h>
+#ifndef WINE_IOS
 #include <servers/bootstrap.h>
 
 /* send our task port to the server */
@@ -1516,6 +1517,7 @@ static void send_server_task_port(void)
 
     mach_port_deallocate(mach_task_self(), wineserver_port);
 }
+#endif  /* !WINE_IOS */
 #endif  /* __APPLE__ */
 
 
@@ -1740,7 +1742,7 @@ void server_init_process_done(void)
         chdir( "/" );
     close( initial_cwd );
 
-#ifdef __APPLE__
+#if defined(__APPLE__) && !defined(WINE_IOS)
     send_server_task_port();
 #endif
 
diff --git a/dlls/ntdll/unix/system.c b/dlls/ntdll/unix/system.c
index 350bcea..0b11324 100644
--- a/dlls/ntdll/unix/system.c
+++ b/dlls/ntdll/unix/system.c
@@ -60,8 +60,10 @@
 #ifdef __APPLE__
 # include <CoreFoundation/CoreFoundation.h>
 # include <IOKit/IOKitLib.h>
+# ifndef WINE_IOS
 # include <IOKit/ps/IOPSKeys.h>
 # include <IOKit/ps/IOPowerSources.h>
+# endif
 # include <mach/mach.h>
 # include <mach/machine.h>
 # include <mach/mach_init.h>
@@ -4243,7 +4245,7 @@ static NTSTATUS fill_battery_state( SYSTEM_BATTERY_STATE *bs )
     return STATUS_SUCCESS;
 }
 
-#elif defined(__APPLE__)
+#elif defined(__APPLE__) && !defined(WINE_IOS)
 
 static NTSTATUS fill_battery_state( SYSTEM_BATTERY_STATE *bs )
 {
