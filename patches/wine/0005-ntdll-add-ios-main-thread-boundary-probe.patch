diff --git a/dlls/ntdll/unix/loader.c b/dlls/ntdll/unix/loader.c
index f2e0edf..98d56e8 100644
--- a/dlls/ntdll/unix/loader.c
+++ b/dlls/ntdll/unix/loader.c
@@ -2323,6 +2323,10 @@ DECLSPEC_EXPORT void __wine_main( int argc, char *argv[] )
 
 #if defined(__APPLE__) && !defined(WINE_IOS)
     apple_main_thread();
+#endif
+#if defined(__APPLE__) && defined(__MACH__) && defined(WINE_IOS)
+    /* iOS host owns the process main thread and its UIKit run loop. */
+    if (wios_main_probe_stop_stage == 4) return;
 #endif
     start_main_thread();
 }
